// sum the number

function sumNumbers(arr){
    var sum = 0
    for(x = 0; x<arr.length; x++){
        sum += arr[x];
    }
    
    console.log(sum);
    
}

sumNumbers([5, 10, 15, 200, 1])




















